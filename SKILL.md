---
name: link2transcript
description: 顺风耳——把任何视频/音频变成文字稿。丢一个链接（YouTube、B站、小红书、X、TikTok、播客实测可用；抖音要自备 cookies）或本地文件进来，出干净的可读文字稿 + 带时间戳版 + 作者/播放/点赞等元数据。适用于「扒一下这个视频的文案」「这条链接讲了什么」「把这个视频转成文字」「拆解一下这个爆款」「这期播客的文字稿」。纯字卡/无口播/关键信息只在画面里的视频加 --frames 抽帧看画面。全本地跑，不依赖豆包/千问等任何厂商的开关。
---

# link2transcript（顺风耳）

一个链接进，一份文字稿出。**全本地、免费、不看任何厂商脸色**——豆包那种"贴链接自动提文案"的功能说关就关，这条管道不会。

技能目录：`$SKILL/`

## 一句话用法

```bash
bash $SKILL/scripts/run.sh "<链接或本地文件>" --lang zh
```

跑完直接把文字稿打印在终端里，同时落盘到 `~/transcripts/<时间戳>/`。

## 参数

| 参数 | 说明 |
|---|---|
| `--lang zh\|en\|auto` | 语种。默认 `auto`（whisper 自己判断）。**明确知道语种就写死，识别更准** |
| `--out 目录` | 指定输出目录，默认 `~/transcripts/<月日-时分秒>/` |
| `--keep-media` | 保留下载的音频和 wav（默认删掉省空间） |
| `--force-asr` | 跳过平台自带字幕，强制用 whisper 重新转录（自带字幕太烂时用） |
| `--head N` | 只处理前 N 秒（快速试探长视频开头的钩子） |
| `--frames` | **开抽帧看画面**（默认关）。下原片 + 均匀抽帧到 `frames/`，见下面「看画面」一节 |
| `--max-frames N` | 手动指定帧数（会自动开 `--frames`）。超过硬上限 30 帧会被压回去 |
| `--frame-width N` | 帧宽度，默认 1024px（能读清画面里的小字）。源片比这窄就按原宽，不放大 |
| `--start`/`--end` | 圈时间段，接受 `秒`、`mm:ss`、`hh:mm:ss`。长视频只抽关键段用这个 |
| `--hint "领域词"` | **转录时**给 whisper 喂一串领域词，让它往这些词上靠。只对慢路（whisper）有用，见下面「专有名词」 |
| `--terms 词表` | **出稿后**按 `错=>对` 词表擦一遍。可给路径，也可给 `terms/` 下的短名；可重复 |
| `--no-terms` | 关掉自动加载的中文默认词表（扒别的领域的中文视频时用） |
| `--cookies-from-browser 浏览器` | 从浏览器取 cookies 传给 yt-dlp（抖音等站点需要）。**默认不开**，cookies 是隐私数据，必须显式指定 |
| `--cookies 文件` | 用 Netscape 格式的 cookies 文件，不碰浏览器 |

## 产物

```
<输出目录>/
  transcript.txt        ← 可读文字稿，按停顿和句子分好段（主要看这个）
  transcript-timed.md   ← 每句带 `mm:ss` 时间戳，要定位原片位置时看
  transcript.srt        ← 字幕文件
  info.md               ← 标题/作者/平台/时长/播放/点赞/评论/原文案（仅链接输入）
  meta.json             ← yt-dlp 原始元数据
  frames/               ← 仅 --frames 时有
    INDEX.md            ← 帧清单（第几帧 / 对应原片时间 / 文件名）+ 读法提示
    frame-01_00m01s.jpg ← 文件名自带原片时间戳，方便引用「00:01 的画面里写着…」
```

## 它怎么工作（两条路自动选）

1. **快路** — 先问平台要自带字幕（YouTube 几乎都有，B站部分有）。有就直接拿，**几秒钟搞定，不动 GPU**。YouTube 的自动字幕是滚动式的（每条重复上一条的尾巴），脚本会自动去重叠。
2. **慢路** — 没字幕就 `yt-dlp` 扒音轨 → `ffmpeg` 转 16k 单声道 → `whisper.cpp` 转录。**约 10 倍实时**（2026-08-08 本机实测：49s 音频 4.9s，196s 音频 19.9s，两次一致；模型加载只占 359ms）。10 分钟的播客约 1 分钟出稿。旧版这里写的「20-60 倍 / 10 分钟约 20 秒」是外推错的，别再引用。

两条路最后都收敛成 SRT，再由 `scripts/srt2text.py` 统一转成可读稿——所以输出格式永远一致。

## 看画面（`--frames`，默认关）

whisper 只有耳朵没有眼睛。**纯字卡视频、无口播演示、关键数字只写在画面上**的内容，光转录一个字都拿不到——这时候加 `--frames`：脚本下原片、均匀抽帧落盘到 `frames/`，你用 `Read` 逐帧读图。

```bash
bash $SKILL/scripts/run.sh "<链接>" --lang zh --frames
```

长视频别整片抽，圈住关键段：

```bash
bash $SKILL/scripts/run.sh "<链接>" --lang en --frames --start 12:30 --end 14:00
```

跑完终端会把 `frames/INDEX.md` 直接打出来，照表格顺序 `Read` 图片就行。

### 什么时候该开（三个触发信号）

1. **转录为空，或短到不像话**——十几秒视频只出几个字。脚本会自己告警。极短结果往往不是"内容少"，是 whisper 在背景音乐上编出来的幻觉（同一条视频实测两次分别编出「字幕志愿者 李宗盛」和「字幕志愿者 杨茜茜」），**绝不能当内容用**。
2. **口播一直说"这个工具/这个项目/这个命令"却从不报名字**——名字大概率打在画面上。
3. **要核对画面里的硬信息**：GitHub star 数、命令行参数、报价数字、UI 长什么样、对比表格。这类东西一律以画面为准，不许拿转录去推。

### 帧预算（规则形状参考 bradautomates/claude-video，帧数按本地口径压小）

| 抽帧区间时长 | 帧数 |
|---|---|
| ≤30 秒 | 10 帧 |
| 30 秒 – 3 分钟 | 20 帧 |
| >3 分钟 | 30 帧（同样帧数摊到更长时间，自动稀疏） |

两条硬上限，`--max-frames` 也压不破：**总数 ≤30 帧**、**平均 ≤2 帧/秒**（3 秒的片子最多 6 帧）。帧间隔超过 20 秒时脚本会警告画面跳得太厉害，那就该用 `--start/--end` 圈小。

宽度默认 1024px——参考项目默认 512px，这里因为要读画面里的字直接上高分辨率；同时长边封顶 1568px（竖屏片再高也没用，读图前会被降采样，白传字节）。**这不便宜**：竖屏 10 帧约 1.2 万 token，抽满 30 帧能到 4-5 万，比一篇长文字稿贵一个量级，所以默认关着，别顺手就加。

### 实现细节（省得踩坑）

- 抽帧排在 whisper 之前：转录再怎么炸，帧已经落盘了。
- 帧取每个时间格的**中点**，躲开开头黑场和转场。
- `--start` 和 `--end` 都给、且输入是链接时，yt-dlp 会裁着下省带宽；脚本会实测本地文件到底是一段还是全片，帧文件名上的时间戳自动对回原片时间轴。**这种情况下转录也只覆盖这一段**。
- 抽帧和转录共用同一份下载的原片，不下两遍；跑完自动删原片（`--keep-media` 保留）。
- 本地文件也能抽：`run.sh ./某视频.mp4 --frames`。纯音频文件会明确报"没有视频流"。

## 专有名词（`--hint` 和 `--terms`）

whisper 把术语、人名、品牌名听错是常态，不是偶发。两道闸，作用在不同阶段，能叠加：

```bash
bash $SKILL/scripts/run.sh "<链接>" --lang zh \
  --hint "幂等 长轮询 序列化 protobuf 熔断 阈值"
```

- **`--hint`＝事前偏向**。这串词作为 initial prompt 喂给 whisper，它解码时会往这些写法上靠。脚本同时开了 `--carry-initial-prompt`（不开的话提示只作用于头 30 秒那一窗，长视频等于没提示）。
- **`--terms`＝事后擦**。出稿前按 `错=>对` 词表改 SRT，`transcript.txt` / `transcript-timed.md` / `transcript.srt` 三份产物口径一致。**每改一处都会在终端打出 `错 → 对 ×N`**，不会偷偷改字。

中文任务（`--lang zh` 或 `auto`）会自动挂 `terms/default-zh.txt`——**仓库里没带这个文件**，因为词表是领域专用的，别人的表对你没用。照着 `terms/example-zh.txt` 建你自己的那份即可；不建也不影响，脚本查不到文件就跳过。

### 实测对照（2026-08-08，large-v3-turbo-q5_0）

原句：「这个接口用了**幂等**设计，走**长轮询**，序列化用 **protobuf**，熔断**阈值设成**五百毫秒。」

| | 转录产物原样 |
|---|---|
| 裸跑 | 这个接口用了**密等**设计，走长轮**巡**，序列化用 **Protubuf**，熔断**玉直射成** 500 毫秒 |
| 只加 `--hint` | 这个接口用了**密等**设计 走长轮询 ✅ 序列化用 protobuf ✅ 熔断 阈值 ✅ **射程** 500 毫秒 |
| `--hint` + 词表 | 这个接口用了幂等设计 走长轮询 序列化用 protobuf 熔断 阈值设成 500 毫秒 |

一句话里 6 个术语，裸跑错 5 个。`--hint` 修好了「长轮询 / protobuf / 阈值」，但「密等」它动不了；补上词表后 6 个术语全对（「五百」变成「500」是数字写法归一，不是错）。

**注意 whisper 不是完全确定性的**：同一段音频跑两次，剩下的错字可能不一样（实测「阈值设成」有时出成「玉直射成」，有时出成「阈值 射程」）。所以词表要按跑出来的报告持续补，别指望一次写全。结论不变——**修不干净是常态，两道闸都要用**。

产物不带标点是 `srt2text.py` 一贯行为（按停顿用空格分句），不是纠错引入的。要标点自己补，或按条读 `transcript.srt`。

### 词表怎么维护

建议分两组写：A 组是你实测命中过的（注明日期），B 组是按发音推的备选。**跑完看终端的纠错报告，发现新错字就往 A 组加**；B 组里哪条真命中了就挪进 A 组。

收录标准只有一条：**左边那个词在你的场景里几乎不可能是对的**。像 `射程=>设成` 这种可以收，`长轮=>长轮询` 这种左边是别的词前缀的一律不收——误伤比漏改贵得多，那种情况交给 `--hint`。

`--hint` 别贪长（30 词以内）：提示词太多 whisper 会开始往里硬塞，反而造出没说过的术语。

## 依赖（都已装好，缺了脚本会报明确的安装命令）

- `yt-dlp` — `uv tool install yt-dlp`
- `ffmpeg` — `brew install ffmpeg`
- `whisper-cli` — `brew install whisper-cpp`
- 模型 `~/.cache/whisper.cpp/ggml-large-v3-turbo-q5_0.bin`（547MB）

## 你（AI）该怎么用它

### 常规：用户丢链接问"这讲了什么"

直接跑脚本，然后**读 `transcript.txt` 给结论**。不要把整篇原文糊给用户——先给一句话结论，再给要点，原文稿放最后或只给路径。

转录为空或短得不像话时**不要就这么回复用户**，加 `--frames` 重跑一遍读画面再回话（见「看画面」）。

**中文里术语密集的内容，跑之前先想一句 `--hint`**（把这条视频大概会提到的术语列出来，30 词以内）。跑完看终端的术语纠错报告，顺手判断有没有漏网的错字。

### 批量：拆一批同类视频

```bash
for u in "链接1" "链接2" "链接3"; do
  bash $SKILL/scripts/run.sh "$u" --lang en --out "/tmp/rip/$(date +%s%N)"
done
```

## 已知边界（别拍胸脯说都能扒）

- **需要登录才能看的内容扒不了**（私密视频、会员内容、部分抖音链接）。yt-dlp 报错时如实告诉用户，别硬试。
- **平台可靠性分三档，别一概说"支持"**（2026-08-08 实测）：
  - **稳**：YouTube、B站、**小红书视频笔记**、本地文件。小红书连跑 3 次全过、每次都是 106 字、零重试——**它和 YouTube 同档，「链接有讲究」是使用约束不是稳定性问题**，两件事别混在一格里说。
  - **TikTok：能用，但靠两件事撑着**。① 脚本给所有 yt-dlp 调用挂了浏览器 UA（`$UA`，可用 env `TRANSCRIPT_UA` 覆盖）——不挂就报 `Unable to extract universal data for rehydration`，TikTok 认得出 yt-dlp 的默认身份，会发一个不含视频数据的空壳页。② 失败后 8 秒 / 20 秒两级重试——TikTok 的 JS 挑战让成功带随机性，**同一条链接交替成功失败，跟加不加 `--download-sections` 无关**（这点最初误判过）。加上这两条后连跑 3 次全过（其中 1 次触发重试）。
  - **抖音：要 cookies，默认扒不动**。2026-08-08 实测两条公开链接都报 `Fresh cookies (not necessarily logged in) are needed`——这是 cookie 墙，不是链接失效。用户加 `--cookies-from-browser chrome`（或 `--cookies 文件`）才可能通。**这条路我没拿真 cookies 验过，只验了参数确实传到了 yt-dlp**，别拍胸脯说抖音能用。
  - **X（Twitter）：稳**。2026-08-08 实测带视频的推文，119s 视频完整转出，连跑 3 次全过、零重试，元数据给得全（作者/平台/时长/发布/播放）。
  - **小红书的链接必须是 App 分享出来的、带 `xsec_token` 的那种**，桌面浏览器地址栏直接复制的链接没有这个参数，会扒不动。token 还会过期，隔一阵子要重新分享一次。
  - **小红书图文笔记**（`type` 不是 video）本身没有视频流，报 `No video formats found` 是正常结果不是坏了；换成视频笔记就能跑。
  - **小红书的元数据只有标题和时长**，作者/播放/点赞/评论全是 `—`（实测确认）。要这些数据得走别的路子，不是脚本坏了。
- **默认只听声音、不看画面**。纯字卡视频、无口播的图文/演示视频、只写在画面上的关键数字，whisper 一个字都拿不到。**遇到这种别停在"转录为空"上报个结论就算完，加 `--frames` 重跑读画面**（用法见上面「看画面」一节）。三个信号见到就该加：① 转录为空或短到不像话 ② 口播只说"这个工具/这个项目"从不报名字 ③ 要核对画面里的 star 数、命令、报价、UI。
- **极短转录比空转录更坑**。空的一眼看得出来，编出来的假句子看不出来——whisper 在纯背景音上会凭空造句（实测「字幕志愿者 李宗盛」）。脚本对 30 字以下的结果会告警，见到告警就当没转录到，改走画面。
- **专有名词会转错**，而且是常态。实测一句技术念白里 6 个术语错 5 个（"幂等"→"密等"、"长轮询"→"长轮巡"、"设成"→"射程"）。词表只覆盖收录过的词——剩下的要按上下文自己纠正，纠正后告诉用户你改了哪些词，并把新错字加进 `terms/default-zh.txt`。术语密集的内容再加 `--hint`（见上面「专有名词」一节）。
- **平台自带字幕不等于准确**。YouTube 自动字幕在口音重、背景音大时错得很离谱，觉得不对劲就加 `--force-asr` 重跑。
- 元数据字段各平台给的不一样（小红书给不出播放/点赞，YouTube 给得全），`info.md` 里显示 `—` 是平台没给，不是脚本坏了。
