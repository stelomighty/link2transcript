# Agent Instructions

这是一个 CLI-first 的视频转文字工具。同一套命令契约适用于 Claude Code、Codex、Cursor、Aider、Cline、Windsurf 等任何能跑 bash 的 agent。

`$SKILL` = 本 skill 的安装目录：

- Claude Code：`~/.claude/skills/link2transcript/`
- Codex：`~/.codex/skills/link2transcript/`
- 其他工具 / 直接 clone：仓库根目录

## 用户给一个视频链接或本地文件时

```bash
bash $SKILL/scripts/run.sh "<链接或路径>" --lang zh --out <输出目录>
```

然后：

1. 读 `<输出目录>/transcript.txt`，**基于它给结论**，不要从链接凭空推断。
2. 要定位原片位置时读 `transcript-timed.md`（每句带 `mm:ss`）。
3. 要标题/作者/播放/点赞等元信息时读 `info.md`。
4. **不要把整篇原文糊给用户**——先一句话结论，再要点，原文放最后或只给路径。

## 三条必须遵守的判断规则

1. **转录为空或短到不像话时，不要就这么回话。** 这多半是纯字卡/无口播视频（模型只有耳朵没有眼睛），加 `--frames` 重跑，然后逐帧读 `frames/` 里的图。索引在 `frames/INDEX.md`。
2. **30 字以下的转录结果按"没转录到"处理。** 模型在纯背景音上会凭空造句，编出来的假句子比空结果更坑。脚本会自己告警。
3. **专有名词几乎一定会错。** 术语密集的内容跑之前加 `--hint "词1 词2 …"`（30 词以内），出稿后按上下文再核一遍，并把新错字加进词表。纠正了哪些词要告诉用户。

## 常用参数

| 参数 | 用途 |
|---|---|
| `--lang zh\|en\|auto` | 语种，知道就写死 |
| `--head N` | 只处理前 N 秒（试探长视频开头） |
| `--frames` | 抽帧看画面（默认关，见规则 1） |
| `--hint "领域词"` | 转录时给模型喂领域词 |
| `--terms 词表` | 出稿前按 `错=>对` 纠错 |
| `--force-asr` | 跳过平台自带字幕强制重转 |

完整说明见 `SKILL.md`，安装和平台注意事项见 `README.md`。

## 别做的事

- 不要把生成的音视频、转录稿、元数据提交进用户的仓库，除非用户明确要留。
- 需要登录的内容扒不了，yt-dlp 报错就如实说，别硬试。
- 看到「隔 8 秒重试」的提示是正常的反爬重试，不是故障。
